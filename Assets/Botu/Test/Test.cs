using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour {

    private Botu botu=new Botu();
	// Use this for initialization
	void Start () {
        Instantiate(botu);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
